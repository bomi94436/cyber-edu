import main, * as mainActions from "./main";

describe("main", () => {
  describe("actions", () => {
    it("should create actions", () => {
      const expectedActions = [{ type: "" }];
    });
  });
});
